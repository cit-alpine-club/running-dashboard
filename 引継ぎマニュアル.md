# ランニング結果ダッシュボード 引継ぎマニュアル

## システム概要

部員がDiscordの「トレーニング」チャンネルにNike Run Club（NRC）のランニング結果画像を投稿すると、毎日自動で集計・グラフ化してWebで閲覧できるシステムです。

**ダッシュボードURL**: https://running-dashboard-ho6x.onrender.com/

---

## 仕組み（全体像）

```
部員がDiscordに画像投稿
       ↓
毎日 02:00 に GitHub Actions が自動実行
       ↓
① Discordから画像を取得
② 画像からOCRで距離・ペース・時間を読み取り
③ CSVに記録してGitHubにプッシュ
       ↓
Renderが自動でWebサイトを更新
       ↓
ダッシュボードに反映
```

**ポイント: 誰かが操作しなくても毎日自動で動きます。**

---

## 管理に必要なアカウント

| アカウント | 用途 | ログイン情報 |
|---|---|---|
| 部のGitHub (`cit-alpine-club`) | ソースコード管理・自動実行 | 部のGoogleアカウントでログイン |
| Render | Webサイトのホスティング | 現在の管理者に確認 |
| Discord Developer Portal | Botの管理 | 部のDiscordアカウントでログイン |

---

## 年次メンテナンス（新入部員が入ったとき）

新しく部員がDiscordサーバーに参加してランニング結果を投稿し始めたら、その部員をダッシュボードに表示するために `name_map.json` を更新します。

### 手順

1. GitHubの `cit-alpine-club/running-dashboard` を開く
2. `name_map.json` を編集
3. DiscordのユーザーID（`@` なしの名前）と部員の氏名を追加

```json
{
  "discord_username": "氏名（例: 山田 太郎）",
  ...
}
```

**DiscordのユーザーIDの確認方法**: Discordの設定 → 詳細設定 → 開発者モードをON → 該当ユーザーを右クリック → 「ユーザーIDをコピー」※ここではユーザー名（英数字）を使う

---

## トラブル対応

### パイプラインが失敗したとき

1. `cit-alpine-club/running-dashboard` をGitHubで開く
2. **Actions** タブ → 最新のワークフローを確認
3. 赤い×がついていたらクリックしてエラー内容を確認
4. **Run workflow** ボタンで手動再実行できる

### ダッシュボードが表示されないとき

1. `https://dashboard.render.com` にログイン
2. `running-dashboard` サービスを選択
3. **Manual Deploy** → **Deploy latest commit** で再デプロイ

### 何日分かデータが欠けているとき

GitHub Actionsのログを確認して失敗した日を特定し、手動で **Run workflow** を実行してください（自動で過去分を取得します）。

---

## 絶対にやってはいけないこと

- **Discord BotをDiscordサーバーから追い出さない**
  - BotはDiscordのメッセージ取得に使用しています。削除するとパイプラインが動かなくなります
- **GitHub Secretsを削除・変更しない**
  - `DISCORD_TOKEN` と `DISCORD_CHANNEL_ID` はパイプラインの認証情報です
  - 変更が必要な場合は Discord Developer Portal でBotトークンを再発行し、Secretsを更新する

---

## GitHub Secretsの更新方法（必要なときのみ）

Botトークンを再発行したときなど：

1. `cit-alpine-club/running-dashboard` → **Settings** → **Secrets and variables** → **Actions**
2. `DISCORD_TOKEN` の鉛筆アイコンをクリック
3. 新しいトークンを貼り付けて **Save changes**

---

## システム構成（技術詳細）

| 項目 | 内容 |
|---|---|
| 自動実行 | GitHub Actions（毎日 02:00 JST） |
| ホスティング | Render（無料プラン） |
| OCRエンジン | Tesseract |
| Discord連携 | Discord Chat Exporter CLI + Bot token |
| リポジトリ | `cit-alpine-club/running-dashboard` |

---

*作成: 2026年5月*
